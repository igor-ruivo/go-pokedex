export interface IRankedPokemon {
    speciesId: string,
    rating: number,
    moveset: string[],
    lead: number,
    switch: number,
    charger: number,
    closer: number,
    consistency: number,
    attacker: number,
    score: number
}